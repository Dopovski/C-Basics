#include <iostream>
#include<iomanip>

using namespace std;

int main() {
    string name;
    int age;
    float grade;

    cout.setf(ios::fixed);
    cout.precision(2);
    cin >> name >> age >> grade;
    cout << "Name: " << name << ", Age: " << age << ", Grade: " << grade << endl;
}
