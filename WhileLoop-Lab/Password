#include <iostream>

using namespace std;

int main() {
    string name;
    cin >> name;

    string password;
    cin >> password;

    string enteredPassword;
    cin >> enteredPassword;

    while (enteredPassword != password) {
        cin >> enteredPassword;
    }
    cout << "Welcome " << name << "!"<< endl;
}
