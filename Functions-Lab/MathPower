#include <iostream>

using namespace std;

int calcPow(int n, int p) {
    int result = 1;
    while (p-- > 0)
        result *= n;
    return result;
}

int main() {
    int n, p;
    cin >> n >> p;

    cout << calcPow(n, p) << endl;
}
