#include <iostream>
#include <iomanip>

using namespace std;

int main() {
    float b1, b2, h;
    cin >> b1 >> b2 >> h;
    double trapezoidArea = (static_cast<double>(b1) + b2) * h / 2.0;
    cout << fixed << setprecision(2) << trapezoidArea << endl;
    return 0;
}
