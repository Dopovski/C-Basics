#include <iostream>
using namespace std;

const int MAX_SIZE_ARRAY = 100;

void readArray(int array[], int size) {
    for (int i = 0; i < size; i++) {
        cin >> array[i];
    }
}

int main() {
    int n;
    cin >> n;

    int array[MAX_SIZE_ARRAY];
    readArray(array, n);

    int max_Length = 1;
    int max_Element = array[0];

    int length = 1;
    int element = array[0];

    for (int i = 1; i < n; i++) {
        if (array[i] == element) {
            length++;
        } else {
            // >= → choose the *last* longest sequence
            if (length >= max_Length) {
                max_Length = length;
                max_Element = element;
            }
            element = array[i];
            length = 1;
        }
    }

    // Final check for last sequence
    if (length >= max_Length) {
        max_Length = length;
        max_Element = element;
    }

    // Print the longest sequence
    for (int i = 0; i < max_Length; i++) {
        cout << max_Element << " ";
    }
    cout << endl;

    return 0;
}
