#include <iostream>
#include <array>
using namespace std;

const int MAX_SIZE_ARRAY = 100;

void readArray(array<int, MAX_SIZE_ARRAY> &arr, int size) {
    for (int i = 0; i < size; i++) {
        cin >> arr[i];
    }
}

void printArray(array<int, MAX_SIZE_ARRAY> &arr, int size) {
    for (int i = 0; i < size; i++) {
        cout << arr[i] << " ";
    }
    cout << endl;
}

void rotateArray(array<int, MAX_SIZE_ARRAY> &arr, int n) {
    int temp = arr[0];
    for (int i = 0; i < n - 1; i++) {
        arr[i] = arr[i + 1];
    }
    arr[n - 1] = temp;
}

int main() {
    int n;
    cin >> n;

    array<int, MAX_SIZE_ARRAY> arr;
    readArray(arr, n);

    int rotation;
    cin >> rotation;

    for (int i = 0; i < rotation; i++) {
        rotateArray(arr, n);
    }

    printArray(arr, n);
}
