#include <iostream>
#include <climits>
#include <array>
using namespace std;

const int MAX_SIZE_ARRAY = 100;

void readArray(int array[], int size) {
    for (int i = 0; i < size; i++) {
        cin >> array[i];
    }
}

int main() {
    int numbers[MAX_SIZE_ARRAY];
    int n;
    cin >> n;
    readArray(numbers, n);

    int maxSoFar = INT_MIN;
    int topElements[MAX_SIZE_ARRAY];
    int index = 0;

    for (int i = n - 1; i >= 0; i--) {
        if (numbers[i] > maxSoFar) {
            topElements[index++] = numbers[i];
            maxSoFar = numbers[i];
        }
    }

    for (int i = index - 1; i >= 0; i--) {
        cout << topElements[i] << " ";
    }
    cout << endl;
}
