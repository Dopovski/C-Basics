#include <iostream>
#include <string>
#include <map>

using namespace std;

int main() {
    map<int, string> companies;

    while (true) {
        string name;
        cin >> name;

        if (name == "end") {
            break;
        }

        int id;
        cin >> id;
        companies[id] = name;
    }

    int searchId;
    cin >> searchId;

    auto it = companies.find(searchId);
    if (it == companies.end()) {
        cout << "[not found]" << endl;
    } else {
        cout << it->second << " " << it->first << endl;
    }

    return 0;
}
